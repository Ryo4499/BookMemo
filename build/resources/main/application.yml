#デプロイ時に変更
spring:
##本番#  datasource:
#    driver-class-name: org.postgresql.Driver
#    username: ${POSTGRES_USER}
#    password: ${POSTGRES_PASS}
#    url: ${DATABASE_URL}
  datasource:
    url: jdbc:postgresql://localhost:5432/bookmemo4444
    username: postgres
    password: p4w0rd
  session:    store-type: jdbc
    jdbc:
      initialize-schema: always
      schema: classpath:org/springframework/session/jdbc/schema-postgresql.sql
    timeout: 1000
  jpa:
    database: POSTGRESQL
    database-platform: org.hibernate.dialect.PostgreSQL94Dialect
    generate-ddl: true
    show-sql: true
    hibernate:      ddl-auto: create-drop    properties:
      '[hibernate.jdbc.time_zone]': Asia/Tokyo
      mapping-resources:
    driver-class-name: org.postgresql.Driver
  h2:
    console:
      enabled: true
  thymeleaf:
    cache: false
        